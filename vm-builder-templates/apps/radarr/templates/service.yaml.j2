---
# Radarr Service (J2 template)
# Defaults match k8s-standalone/base/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: {{ app_name | default('radarr') }}
  namespace: {{ app_namespace | default('media-stack') }}
  labels:
    app: {{ app_name | default('radarr') }}
    app.kubernetes.io/name: {{ app_name | default('radarr') }}
    app.kubernetes.io/part-of: media-stack
spec:
  type: {{ service_type | default('NodePort') }}
  selector:
    app: {{ app_name | default('radarr') }}
  ports:
    - name: http
      port: {{ container_port | default(7878) }}
      targetPort: {{ container_port | default(7878) }}
{% if service_type | default('NodePort') == 'NodePort' %}
      nodePort: {{ nodeport | default(30878) }}
{% endif %}
      protocol: TCP
