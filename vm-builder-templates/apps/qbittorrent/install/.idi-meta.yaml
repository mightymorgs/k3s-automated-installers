schema_version: '1.0'
repo_id: vm-builder-templates
files:
  01-deploy.yml:
    summary: Deploy qBittorrent torrent client to K3s cluster. Requires shared media
      storage. Accessible via Tailscale on NodePort 30081.
    source: inferred
    confidence: medium
    uses_files:
    - '{{ templates_dir }}/statefulset.yaml.j2'
    - '{{ templates_dir }}/service.yaml.j2'
  02-verify.yml:
    summary: Verify qBittorrent deployment is healthy
    source: inferred
    confidence: medium
    wiring:
      reads_from: []
      writes_to:
      - app: qbittorrent
        field: verified
