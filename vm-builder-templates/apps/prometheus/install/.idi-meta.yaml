schema_version: '1.0'
repo_id: vm-builder-templates
files:
  01-deploy.yml:
    summary: Deploy Prometheus metrics collection system to K3s cluster. Scrapes metrics
      from all cluster workloads. Accessible via Tailscale on NodePort 30090.
    source: inferred
    confidence: medium
    uses_files:
    - '{{ templates_dir }}/namespace.yaml.j2'
    - '{{ templates_dir }}/rbac.yaml.j2'
    - '{{ templates_dir }}/configmap.yaml.j2'
    - '{{ templates_dir }}/statefulset.yaml.j2'
    - '{{ templates_dir }}/service.yaml.j2'
