---
# playbook_metadata:
#   id: "atomic/apps/elastic/01-apply-kustomize"
#   type: "atomic"
#
#   # ========== FILE LOCATION ==========
#   path_info:
#     file_path: "atomic/apps/elastic/install/01-apply-kustomize.yml"
#     category: "apps"
#     subsystem: "elastic"
#
#   # ========== VERSION COMPATIBILITY ==========
#   compatibility:
#     primary_component:
#       name: "elastic"
#       supported_versions: ["latest"]
#       version_range: ">=1.0.0"
#
#     requires:
#       k3s: ">=1.30.0"
#       longhorn: ">=1.6.0"
#       os:
#         ubuntu: ">=22.04"
#         debian: ">=12"
#
#     tested_combinations:
#       - elastic: "latest"
#         k3s: "1.33.x"
#         os: "ubuntu-24.04"
#         result: "pending"
#         test_date: "2026-01-28"
#         notes: "Generated by IDI compiler"
#
#   # ========== TRADITIONAL METADATA ==========
#   batch_id: "20260128_elastic_install"
#   dependencies: ["k3s_server_installed"]
#   provides: ["elastic_deployed", "elastic_namespace_ready"]
#   tags: ["elastic", "kustomize", "golden-master"]
#   estimated_duration: 180
#   parallel_safe: false
#   idempotent: true
#   description: Deploy Elasticsearch via Kustomize
#
#   # ========== CREDENTIALS (v3.2) ==========
#   credentials:
#     secret_name: "elasticsearch-credentials"
#     fields:
#       password:
#         bws_field: "admin_password"
#         source: "k8s-secret"
#
#   # ========== BWS STATE INTERACTION (v3.2) ==========
#   bws_state:
#     reads:
#       - path: "_state.vault.url"
#         purpose: "ESO ClusterSecretStore endpoint"
#       - path: "inventory/shared/secrets/elastic/admin_password"
#         purpose: "Admin password"
#     writes:
#       - path: "_state.apps.elastic.status"
#         example: "ready"
#       - path: "_state.apps.elastic.url"
#         example: "https://elastic.example.com"
#       - path: "_state.apps.elastic.nodeport"
#         example: 30920
#
#   # ========== INGRESS CONFIGURATION (v3.2) ==========
#   ingress:
#     enabled: true
#     subdomain: "elastic"
#     service_name: "elasticsearch"
#     service_port: 9200
#     namespace: "logging"
#     mode_from_inventory: "ingress.mode"
#     cloudflare:
#       tunnel_name: "{client}-tunnel"
#       domain_from_inventory: "ingress.domain"
#       origin_cert:
#         enabled: true
#         api_token_path: "inventory/shared/secrets/cloudflare/apitoken"
#         cert_validity_days: 3650
#     tailscale:
#       in_cluster: true
#       cert:
#         method: "tailscale-cert"
#     paths:
#       - path: "/"
#         path_type: "Prefix"
#
#   # ========== SSO CONFIGURATION (v3.2) ==========
#   sso:
#     enabled: true
#     provider: "authentik"
#     forwardauth:
#       enabled: true
#       trust_forward_header: true
#       auth_url_template:
#         cloudflare: "https://{domain}/outpost.goauthentik.io/auth/traefik"
#         tailscale: "https://authentik.{tailnet}.ts.net/outpost.goauthentik.io/auth/traefik"
#     bypass_paths:
#       - "/api"
#       - "/_cluster"
#
#   # ========== UI METADATA ==========
#   ui:
#     display_name: "Elasticsearch"
#     icon: "search"
#     category: "Logging"
#     description: "Distributed search and analytics engine"
#     docs_url: "https://www.elastic.co/guide/en/elasticsearch/reference/current/index.html"
#     port: 9200
#     nodeport: 30920
#
#   # ========== CONFIGURABLE VARIABLES ==========
#   vars:
#     app_name:
#       label: "App Name"
#       description: "Kubernetes resource name"
#       type: "string"
#       default: "elastic"
#     app_namespace:
#       label: "Namespace"
#       description: "Kubernetes namespace"
#       type: "string"
#       default: "elastic"
#     app_component:
#       label: "App Component"
#       description: "Kubernetes app component label"
#       type: "string"
#       default: "server"
#     image:
#       label: "Container Image"
#       description: "Docker image for Elasticsearch"
#       type: "string"
#       default: "docker.elastic.co/elasticsearch/elasticsearch:8.16.1"
#     image_pull_policy:
#       label: "Image Pull Policy"
#       description: "Kubernetes image pull policy"
#       type: "choice"
#       default: "IfNotPresent"
#       options: ["Always", "IfNotPresent", "Never"]
#     replicas:
#       label: "Replicas"
#       description: "Number of Elasticsearch replicas"
#       type: "integer"
#       default: 1
#     container_port:
#       label: "Container Port"
#       description: "Elasticsearch HTTP port"
#       type: "integer"
#       default: 9200
#     configmap_name:
#       label: "ConfigMap Name"
#       description: "Name of the Kubernetes ConfigMap for Elasticsearch env vars"
#       type: "string"
#       default: "elastic-config"
#     secret_name:
#       label: "Secret Name"
#       description: "Name of the Kubernetes Secret for Elasticsearch credentials"
#       type: "string"
#       default: "elastic-secrets"
#     service_type:
#       label: "Service Type"
#       description: "Kubernetes service type"
#       type: "choice"
#       default: "NodePort"
#       options: ["NodePort", "ClusterIP", "LoadBalancer"]
#     nodeport:
#       label: "NodePort"
#       description: "External port for access via Tailscale IP"
#       type: "integer"
#       default: 30920
#     data_mount_path:
#       label: "Data Mount Path"
#       description: "Container path for Elasticsearch data volume"
#       type: "string"
#       default: "/data"
#     cpu_request:
#       label: "CPU Request"
#       description: "Minimum CPU allocation"
#       type: "string"
#       default: "500m"
#     memory_request:
#       label: "Memory Request"
#       description: "Minimum memory allocation"
#       type: "string"
#       default: "1Gi"
#     cpu_limit:
#       label: "CPU Limit"
#       description: "Maximum CPU allocation"
#       type: "string"
#       default: "2000m"
#     memory_limit:
#       label: "Memory Limit"
#       description: "Maximum memory allocation"
#       type: "string"
#       default: "2Gi"
#     health_path:
#       label: "Health Check Path"
#       description: "HTTP path for liveness and readiness probes"
#       type: "string"
#       default: "/_cluster/health"
#     liveness_initial_delay:
#       label: "Liveness Initial Delay"
#       description: "Seconds before first liveness probe"
#       type: "integer"
#       default: 60
#     readiness_initial_delay:
#       label: "Readiness Initial Delay"
#       description: "Seconds before first readiness probe"
#       type: "integer"
#       default: 30
#     storage_class:
#       label: "Storage Class"
#       description: "StorageClass for PVCs"
#       type: "string"
#       default: "longhorn"
#     data_storage_size:
#       label: "Data Storage"
#       description: "Persistent volume size for Elasticsearch data"
#       type: "string"
#       default: "10Gi"
#     java_opts:
#       label: "Java Options"
#       description: "JVM options for Elasticsearch"
#       type: "string"
#       default: "-Xms512m -Xmx512m"

- name: Deploy Elasticsearch
  hosts: "{{ target_hosts | default('localhost') }}"
  gather_facts: true
  become: false

  vars:
    app_name: elastic
    app_namespace: "elastic"
    templates_dir: "{{ playbook_dir }}/../templates"
    render_dir: "/tmp/elastic-manifests"
    kubectl_action: "{{ 'replace --force' if install_mode | default('rebuild') == 'full' else 'apply' }}"
    container_port: 9200
    health_path: "/_cluster/health"
    app_start_timeout_seconds: 600
    retry_interval_seconds: 10
    health_probe_timeout_seconds: 5

  tasks:
    - name: Verify kubectl is available
      ansible.builtin.command: kubectl version --client
      register: kubectl_check
      changed_when: false

    - name: Verify K3s cluster is accessible
      ansible.builtin.command: kubectl get nodes
      register: nodes_check
      changed_when: false
      retries: 5
      delay: "{{ retry_interval_seconds }}"
      until: nodes_check.rc == 0

    - name: Create render directory
      ansible.builtin.file:
        path: "{{ render_dir }}"
        state: directory
        mode: '0755'

    - name: Render ConfigMap template
      ansible.builtin.template:
        src: "{{ templates_dir }}/configmap.yaml.j2"
        dest: "{{ render_dir }}/configmap.yaml"
        mode: '0644'

    - name: Render StatefulSet template
      ansible.builtin.template:
        src: "{{ templates_dir }}/statefulset.yaml.j2"
        dest: "{{ render_dir }}/statefulset.yaml"
        mode: '0644'

    - name: Render Service template
      ansible.builtin.template:
        src: "{{ templates_dir }}/service.yaml.j2"
        dest: "{{ render_dir }}/service.yaml"
        mode: '0644'

    - name: Render Elasticsearch ClusterIP Service template
      ansible.builtin.template:
        src: "{{ templates_dir }}/elasticsearch-service.yaml.j2"
        dest: "{{ render_dir }}/elasticsearch-service.yaml"
        mode: '0644'

    - name: Render Kibana Deployment template
      ansible.builtin.template:
        src: "{{ templates_dir }}/kibana-deployment.yaml.j2"
        dest: "{{ render_dir }}/kibana-deployment.yaml"
        mode: '0644'

    - name: Render Kibana Service template
      ansible.builtin.template:
        src: "{{ templates_dir }}/kibana-service.yaml.j2"
        dest: "{{ render_dir }}/kibana-service.yaml"
        mode: '0644'

    - name: Render PushSecret template
      ansible.builtin.template:
        src: "{{ templates_dir }}/pushsecret.yaml.j2"
        dest: "{{ render_dir }}/pushsecret.yaml"
        mode: '0644'

    - name: Ensure namespace exists
      ansible.builtin.shell: |
        kubectl create namespace {{ app_namespace }} --dry-run=client -o yaml | kubectl apply -f -
      register: ns_apply
      changed_when: "'created' in ns_apply.stdout or 'configured' in ns_apply.stdout"

    - name: Label namespace
      ansible.builtin.command: >-
        kubectl label namespace {{ app_namespace }}
        app.kubernetes.io/name={{ app_name }}
        app.kubernetes.io/part-of=golden-master
        --overwrite
      changed_when: false

    - name: Apply PushSecret (always idempotent)
      ansible.builtin.command:
        cmd: kubectl apply -f {{ render_dir }}/pushsecret.yaml
      register: pushsecret_apply
      changed_when: "'created' in pushsecret_apply.stdout or 'configured' in pushsecret_apply.stdout"

    - name: Apply remaining manifests
      ansible.builtin.command:
        cmd: >-
          kubectl {{ kubectl_action }}
          -f {{ render_dir }}/configmap.yaml
          -f {{ render_dir }}/statefulset.yaml
          -f {{ render_dir }}/service.yaml
          -f {{ render_dir }}/elasticsearch-service.yaml
          -f {{ render_dir }}/kibana-deployment.yaml
          -f {{ render_dir }}/kibana-service.yaml
      register: manifests_apply
      changed_when: "'created' in manifests_apply.stdout or 'configured' in manifests_apply.stdout"

    - name: Wait for StatefulSet to create pod
      ansible.builtin.command: >-
        kubectl -n {{ app_namespace }} get pod -l app={{ app_name }} --no-headers
      register: pod_exists
      changed_when: false
      retries: "{{ (app_start_timeout_seconds / retry_interval_seconds) | int }}"
      delay: "{{ retry_interval_seconds }}"
      until: pod_exists.rc == 0 and pod_exists.stdout | length > 0

    - name: Wait for Elasticsearch pod to be ready
      ansible.builtin.command: >-
        kubectl -n {{ app_namespace }} wait
        --for=condition=ready pod
        -l app={{ app_name }}
        --timeout={{ app_start_timeout_seconds }}s
      register: pod_wait
      changed_when: false
      retries: 3
      delay: "{{ retry_interval_seconds }}"
      until: pod_wait.rc == 0

    - name: Get Tailscale IP for access URL
      ansible.builtin.command: tailscale ip -4
      register: tailscale_ip
      changed_when: false
      failed_when: false

    - name: Display deployment status
      ansible.builtin.debug:
        msg:
          - "=========================================="
          - "Elasticsearch Deployed Successfully"
          - "=========================================="
          - "Namespace: {{ app_namespace }}"
          - "Image: {{ image | default('docker.elastic.co/elasticsearch/elasticsearch:8.16.1') }}"
          - "Access URL: http://{{ tailscale_ip.stdout | default(ansible_hostname) }}:{{ nodeport | default(30920) }}"
          - "=========================================="

    - name: Clean up render directory
      ansible.builtin.file:
        path: "{{ render_dir }}"
        state: absent
