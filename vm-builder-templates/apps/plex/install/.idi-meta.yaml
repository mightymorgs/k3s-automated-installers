schema_version: '1.0'
repo_id: vm-builder-templates
files:
  01-deploy.yml:
    summary: Deploy Plex media server to K3s cluster via J2 templates. Requires Longhorn
      storage. Accessible via NodePort 32400.
    source: inferred
    confidence: medium
    uses_files:
    - '{{ templates_dir }}/configmap.yaml.j2'
    - '{{ templates_dir }}/statefulset.yaml.j2'
    - '{{ templates_dir }}/service.yaml.j2'
  02-verify.yml:
    summary: Verify Plex deployment is healthy
    source: inferred
    confidence: medium
