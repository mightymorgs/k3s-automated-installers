---
# Plex Service (J2 template)
# Defaults match k8s-base/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: {{ app_name | default('plex') }}
  namespace: {{ app_namespace | default('plex') }}
  labels:
    app: {{ app_name | default('plex') }}
    app.kubernetes.io/name: {{ app_name | default('plex') }}
spec:
  type: {{ service_type | default('NodePort') }}
  selector:
    app: {{ app_name | default('plex') }}
  ports:
    - name: http
      port: {{ container_port | default(32400) }}
      targetPort: {{ container_port | default(32400) }}
{% if service_type | default('NodePort') == 'NodePort' %}
      nodePort: {{ nodeport | default(32400) }}
{% endif %}
      protocol: TCP
