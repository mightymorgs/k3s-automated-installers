---
# Plex StatefulSet (J2 template)
# Defaults match k8s-base/statefulset.yaml
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ app_name | default('plex') }}
  namespace: {{ app_namespace | default('plex') }}
  labels:
    app: {{ app_name | default('plex') }}
    app.kubernetes.io/name: {{ app_name | default('plex') }}
    app.kubernetes.io/component: {{ app_component | default('server') }}
spec:
  serviceName: {{ app_name | default('plex') }}
  replicas: {{ replicas | default(1) }}
  selector:
    matchLabels:
      app: {{ app_name | default('plex') }}
  template:
    metadata:
      labels:
        app: {{ app_name | default('plex') }}
        app.kubernetes.io/name: {{ app_name | default('plex') }}
        app.kubernetes.io/component: {{ app_component | default('server') }}
    spec:
      containers:
        - name: {{ app_name | default('plex') }}
          image: {{ image | default('plexinc/pms-docker:latest') }}
          imagePullPolicy: {{ image_pull_policy | default('IfNotPresent') }}
          ports:
            - name: http
              containerPort: {{ container_port | default(32400) }}
              protocol: TCP
          envFrom:
            - configMapRef:
                name: {{ configmap_name | default('plex-config') }}
            - secretRef:
                name: {{ secret_name | default('plex-secrets') }}
                optional: true
          volumeMounts:
            - name: data
              mountPath: {{ data_mount_path | default('/data') }}
          resources:
            requests:
              cpu: {{ cpu_request | default('100m') }}
              memory: {{ memory_request | default('256Mi') }}
            limits:
              cpu: {{ cpu_limit | default('2000m') }}
              memory: {{ memory_limit | default('2Gi') }}
          livenessProbe:
            httpGet:
              path: {{ health_path | default('/web/index.html') }}
              port: http
            initialDelaySeconds: {{ liveness_initial_delay | default(60) }}
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: {{ health_path | default('/web/index.html') }}
              port: http
            initialDelaySeconds: {{ readiness_initial_delay | default(30) }}
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
  volumeClaimTemplates:
    - metadata:
        name: data
        labels:
          app: {{ app_name | default('plex') }}
      spec:
        accessModes: ["ReadWriteOnce"]
        storageClassName: {{ storage_class | default('longhorn') }}
        resources:
          requests:
            storage: {{ data_storage_size | default('10Gi') }}
