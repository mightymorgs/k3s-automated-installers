---
apiVersion: v1
kind: Service
metadata:
  name: plex
  namespace: plex
  labels:
    app.kubernetes.io/name: plex
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: plex
  ports:
    - port: 32400
      targetPort: http
      protocol: TCP
      name: http
      nodePort: 32400
