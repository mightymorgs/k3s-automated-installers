schema_version: '1.0'
repo_id: vm-builder-templates
files:
  01-deploy.yml:
    summary: Deploy Jellyfin media server to K3s cluster via J2 templates. Accessible
      via NodePort 30809.
    source: inferred
    confidence: medium
    uses_files:
    - '{{ templates_dir }}/configmap.yaml.j2'
    - '{{ templates_dir }}/statefulset.yaml.j2'
    - '{{ templates_dir }}/service.yaml.j2'
  02-verify.yml:
    summary: Verify Jellyfin deployment is healthy
    source: inferred
    confidence: medium
