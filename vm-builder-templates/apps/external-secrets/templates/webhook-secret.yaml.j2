{#
  External Secrets Operator — Webhook TLS Secret (empty skeleton)

  This empty Secret is created as a placeholder. The cert-controller
  populates it with the generated TLS certificate and key for the webhook.

  Variables:
    - app_namespace: Kubernetes namespace (default: "external-secrets")
#}
---
apiVersion: v1
kind: Secret
metadata:
  name: external-secrets-webhook
  namespace: {{ app_namespace | default('external-secrets') }}
  labels:
    app.kubernetes.io/name: external-secrets-webhook
    app.kubernetes.io/instance: external-secrets
    app.kubernetes.io/version: {{ eso_version | default('v0.9.13') | quote }}
    app.kubernetes.io/component: webhook
    app.kubernetes.io/part-of: golden-master
    external-secrets.io/component: webhook
