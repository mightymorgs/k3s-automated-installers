---
# playbook_metadata:
#   id: "atomic/apps/teleport/03-verify"
#   type: "atomic"
#
#   # ========== FILE LOCATION ==========
#   path_info:
#     file_path: "atomic/apps/teleport/install/02-verify.yml"
#     category: "platform"
#     subsystem: "teleport"
#
#   # ========== VERSION COMPATIBILITY ==========
#   compatibility:
#     primary_component:
#       name: "teleport"
#       supported_versions: ["15.x", "16.x", "17.x"]
#       version_range: ">=15.0.0"
#
#     requires:
#       k3s: ">=1.28.0"
#       os:
#         ubuntu: ">=22.04"
#
#     tested_combinations:
#       - teleport: "17.x"
#         os: "ubuntu-24.04"
#         result: "pending"
#         test_date: "2026-01-04"
#         notes: "Pending functional test on target infrastructure"
#
#   # ========== TRADITIONAL METADATA ==========
#   batch_id: "20260123_v32_bws_state_migration"
#   dependencies: ["teleport_manifests_applied"]
#   provides: ["teleport_verified", "teleport_ready"]
#   tags: ["teleport", "verification", "health-check", "golden-master"]
#   estimated_duration: 30
#   parallel_safe: true
#   idempotent: true
#   description: Verify Teleport is healthy and accessible
#
#   # ========== BWS STATE INTERACTION (v3.2 NEW) ==========
#   bws_state:
#     # What BWS paths this playbook reads from
#     reads:
#       - path: "_state.apps.teleport.status"
#         purpose: "Check if Teleport is already verified"
#
#     # What BWS paths this playbook writes to
#     writes:
#       - path: "_state.apps.teleport.status"
#         example: "verified"

- name: Verify Teleport Health
  hosts: "{{ target_hosts | default('localhost') }}"
  gather_facts: false
  become: false

  vars:
    app_name: teleport
    app_namespace: "teleport"

    # Global timeout constants
    health_probe_timeout_seconds: 5
    retry_interval_seconds: 10

  tasks:
    # ========== 1. POD PHASE CHECK ==========
    - name: Check pod phase is Running
      ansible.builtin.command: >-
        kubectl -n {{ app_namespace }} get pods
        -l app.kubernetes.io/name={{ app_name }}
        -o jsonpath='{.items[0].status.phase}'
      register: pod_phase
      changed_when: false
      failed_when: pod_phase.rc != 0 or pod_phase.stdout != 'Running'
      retries: 6
      delay: "{{ retry_interval_seconds }}"
      until: pod_phase.rc == 0 and pod_phase.stdout == 'Running'

    # ========== 2. GET READY POD NAME ==========
    - name: Get ready pod name
      ansible.builtin.shell: >-
        kubectl -n {{ app_namespace }} get pods
        -l app.kubernetes.io/name={{ app_name }}
        -o jsonpath='{.items[?(@.status.containerStatuses[0].ready==true)].metadata.name}'
        | awk '{print $1}'
      register: app_pod
      changed_when: false
      retries: 3
      delay: "{{ retry_interval_seconds }}"
      until: app_pod.rc == 0 and app_pod.stdout != ''

    # ========== 3. GET TELEPORT CLUSTER STATUS ==========
    - name: Get Teleport cluster status
      ansible.builtin.command: >-
        kubectl exec -n {{ app_namespace }} {{ app_pod.stdout }}
        -- tctl status
      register: teleport_status_raw
      changed_when: false
      failed_when: teleport_status_raw.rc != 0
      retries: 3
      delay: "{{ retry_interval_seconds }}"
      until: teleport_status_raw.rc == 0

    # ========== 4. SERVICE EXISTENCE CHECK ==========
    - name: Check service exists
      ansible.builtin.command: >-
        kubectl -n {{ app_namespace }} get svc {{ app_name }}
        -o jsonpath='{.spec.type}'
      register: service_check
      changed_when: false
      failed_when: service_check.stdout == ''

    # ========== 5. DISPLAY VERIFICATION SUMMARY ==========
    - name: Display verification summary
      ansible.builtin.debug:
        msg:
          - "=========================================="
          - "{{ app_name }} Verification Summary"
          - "=========================================="
          - "Pod Phase:      {{ pod_phase.stdout | default('UNKNOWN') }} — {{ 'OK' if pod_phase.stdout == 'Running' else 'FAILED' }}"
          - "Ready Pod:      {{ app_pod.stdout | default('NONE') }} — {{ 'OK' if app_pod.stdout | default('') != '' else 'FAILED' }}"
          - "Cluster Status: {{ 'OK' if teleport_status_raw.rc == 0 else 'FAILED' }}"
          - "Service:        {{ service_check.stdout | default('NONE') }} — {{ 'OK' if service_check.stdout | default('') != '' else 'FAILED' }}"
          - "=========================================="
