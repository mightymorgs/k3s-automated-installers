---
# Teleport Namespace (J2 template)
# Defaults match k8s-base/namespace.yaml
apiVersion: v1
kind: Namespace
metadata:
  name: {{ app_namespace | default('teleport') }}
  labels:
    app.kubernetes.io/name: {{ app_name | default('teleport') }}
    app.kubernetes.io/part-of: golden-master
