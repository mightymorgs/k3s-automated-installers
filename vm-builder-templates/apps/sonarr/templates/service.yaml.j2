---
# Sonarr Service (J2 template)
# Defaults match k8s-standalone/base/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: {{ app_name | default('sonarr') }}
  namespace: {{ app_namespace | default('media-stack') }}
  labels:
    app: {{ app_name | default('sonarr') }}
    app.kubernetes.io/name: {{ app_name | default('sonarr') }}
    app.kubernetes.io/part-of: media-stack
spec:
  type: {{ service_type | default('NodePort') }}
  selector:
    app: {{ app_name | default('sonarr') }}
  ports:
    - name: http
      port: {{ container_port | default(8989) }}
      targetPort: {{ container_port | default(8989) }}
{% if service_type | default('NodePort') == 'NodePort' %}
      nodePort: {{ nodeport | default(30989) }}
{% endif %}
      protocol: TCP
