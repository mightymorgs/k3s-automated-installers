schema_version: '1.0'
repo_id: vm-builder-templates
files:
  01-deploy.yml:
    summary: Deploy Prowlarr indexer manager to K3s cluster via J2 templates. Manages
      indexers for *arr apps. Accessible via NodePort 30696.
    source: inferred
    confidence: medium
    uses_files:
    - '{{ templates_dir }}/statefulset.yaml.j2'
    - '{{ templates_dir }}/service.yaml.j2'
  02-verify.yml:
    summary: Verify Prowlarr deployment is healthy
    source: inferred
    confidence: medium
    wiring:
      reads_from: []
      writes_to:
      - app: prowlarr
        field: verified
