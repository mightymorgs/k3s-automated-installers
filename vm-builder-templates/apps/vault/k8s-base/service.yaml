---
apiVersion: v1
kind: Service
metadata:
  name: vault
  namespace: vault
  labels:
    app.kubernetes.io/name: vault
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: vault
  ports:
    - port: 8200
      targetPort: http
      protocol: TCP
      name: http
      nodePort: 30820
