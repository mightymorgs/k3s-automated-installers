schema_version: '1.0'
repo_id: vm-builder-templates
files:
  01-deploy.yml:
    summary: Deploy Loki log aggregation system to K3s cluster. Provides centralized
      logging for all cluster workloads. Accessible via Tailscale on NodePort 30100.
    source: inferred
    confidence: medium
    uses_files:
    - '{{ templates_dir }}/configmap.yaml.j2'
    - '{{ templates_dir }}/statefulset.yaml.j2'
    - '{{ templates_dir }}/service.yaml.j2'
