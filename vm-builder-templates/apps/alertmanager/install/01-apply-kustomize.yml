---
# playbook_metadata:
#   id: "atomic/apps/alertmanager/01-apply-kustomize"
#   type: "atomic"
#
#   # ========== FILE LOCATION ==========
#   path_info:
#     file_path: "atomic/apps/alertmanager/install/01-apply-kustomize.yml"
#     category: "apps"
#     subsystem: "alertmanager"
#
#   # ========== VERSION COMPATIBILITY ==========
#   compatibility:
#     primary_component:
#       name: "alertmanager"
#       supported_versions: ["latest"]
#       version_range: ">=1.0.0"
#
#     requires:
#       k3s: ">=1.30.0"
#       longhorn: ">=1.6.0"
#       os:
#         ubuntu: ">=22.04"
#         debian: ">=12"
#
#     tested_combinations:
#       - alertmanager: "latest"
#         k3s: "1.33.x"
#         os: "ubuntu-24.04"
#         result: "pending"
#         test_date: "2026-01-28"
#         notes: "Generated by IDI compiler"
#
#   # ========== TRADITIONAL METADATA ==========
#   batch_id: "20260128_alertmanager_install"
#   dependencies: ["k3s_server_installed"]
#   provides: ["alertmanager_deployed", "alertmanager_namespace_ready"]
#   tags: ["alertmanager", "kustomize", "golden-master"]
#   estimated_duration: 180
#   parallel_safe: false
#   idempotent: true
#   description: Deploy Alertmanager via Kustomize
#
#   # ========== BWS STATE INTERACTION (v3.2) ==========
#   bws_state:
#     reads:
#       - path: "_state.vault.url"
#         purpose: "ESO ClusterSecretStore endpoint"
#       - path: "inventory/shared/secrets/alertmanager/admin_password"
#         purpose: "Admin password"
#     writes:
#       - path: "_state.apps.alertmanager.status"
#         example: "ready"
#       - path: "_state.apps.alertmanager.url"
#         example: "https://alertmanager.example.com"
#       - path: "_state.apps.alertmanager.nodeport"
#         example: 30903
#
#   # ========== INGRESS CONFIGURATION (v3.2) ==========
#   ingress:
#     enabled: true
#     subdomain: "alertmanager"
#     service_name: "alertmanager"
#     service_port: 9093
#     namespace: "observability"
#     mode_from_inventory: "ingress.mode"
#     cloudflare:
#       tunnel_name: "{client}-tunnel"
#       domain_from_inventory: "ingress.domain"
#       origin_cert:
#         enabled: true
#         api_token_path: "inventory/shared/secrets/cloudflare/apitoken"
#         cert_validity_days: 3650
#     tailscale:
#       in_cluster: true
#       cert:
#         method: "tailscale-cert"
#     paths:
#       - path: "/"
#         path_type: "Prefix"
#
#   # ========== SSO CONFIGURATION (v3.2) ==========
#   sso:
#     enabled: true
#     provider: "authentik"
#     forwardauth:
#       enabled: true
#       trust_forward_header: true
#       auth_url_template:
#         cloudflare: "https://{domain}/outpost.goauthentik.io/auth/traefik"
#         tailscale: "https://authentik.{tailnet}.ts.net/outpost.goauthentik.io/auth/traefik"
#     bypass_paths:
#       - "/api"
#
#   # ========== UI METADATA ==========
#   ui:
#     display_name: "Alertmanager"
#     icon: "bell"
#     category: "Observability"
#     description: "Alert management and routing for Prometheus"
#     docs_url: "https://prometheus.io/docs/alerting/latest/alertmanager/"
#     port: 9093
#     nodeport: 30903
#
#   # ========== CONFIGURABLE VARIABLES ==========
#   vars:
#     app_name:
#       label: "App Name"
#       description: "Kubernetes resource name"
#       type: "string"
#       default: "alertmanager"
#     app_namespace:
#       label: "Namespace"
#       description: "Kubernetes namespace"
#       type: "string"
#       default: "alertmanager"
#     image:
#       label: "Container Image"
#       description: "Docker image for Alertmanager"
#       type: "string"
#       default: "prom/alertmanager:v0.27.0"
#     image_pull_policy:
#       label: "Image Pull Policy"
#       description: "Kubernetes image pull policy"
#       type: "choice"
#       default: "IfNotPresent"
#       options: ["Always", "IfNotPresent", "Never"]
#     replicas:
#       label: "Replicas"
#       description: "Number of Alertmanager replicas"
#       type: "integer"
#       default: 1
#     container_port:
#       label: "Container Port"
#       description: "Alertmanager HTTP port"
#       type: "integer"
#       default: 9093
#     configmap_name:
#       label: "ConfigMap Name"
#       description: "Name of the Kubernetes ConfigMap for Alertmanager env vars"
#       type: "string"
#       default: "alertmanager-config"
#     secret_name:
#       label: "Secret Name"
#       description: "Name of the Kubernetes Secret for Alertmanager credentials"
#       type: "string"
#       default: "alertmanager-secrets"
#     service_type:
#       label: "Service Type"
#       description: "Kubernetes service type"
#       type: "choice"
#       default: "NodePort"
#       options: ["NodePort", "ClusterIP", "LoadBalancer"]
#     nodeport:
#       label: "NodePort"
#       description: "External port for access via Tailscale IP"
#       type: "integer"
#       default: 30903
#     data_mount_path:
#       label: "Data Mount Path"
#       description: "Container path for Alertmanager data volume"
#       type: "string"
#       default: "/data"
#     cpu_request:
#       label: "CPU Request"
#       description: "Minimum CPU allocation"
#       type: "string"
#       default: "100m"
#     memory_request:
#       label: "Memory Request"
#       description: "Minimum memory allocation"
#       type: "string"
#       default: "256Mi"
#     cpu_limit:
#       label: "CPU Limit"
#       description: "Maximum CPU allocation"
#       type: "string"
#       default: "2000m"
#     memory_limit:
#       label: "Memory Limit"
#       description: "Maximum memory allocation"
#       type: "string"
#       default: "2Gi"
#     health_path:
#       label: "Health Check Path"
#       description: "HTTP path for liveness probe"
#       type: "string"
#       default: "/-/healthy"
#     ready_path:
#       label: "Readiness Check Path"
#       description: "HTTP path for readiness probe"
#       type: "string"
#       default: "/-/ready"
#     liveness_initial_delay:
#       label: "Liveness Initial Delay"
#       description: "Seconds before first liveness probe"
#       type: "integer"
#       default: 30
#     readiness_initial_delay:
#       label: "Readiness Initial Delay"
#       description: "Seconds before first readiness probe"
#       type: "integer"
#       default: 10
#     storage_class:
#       label: "Storage Class"
#       description: "StorageClass for PVCs"
#       type: "string"
#       default: "longhorn"
#     data_storage_size:
#       label: "Data Storage"
#       description: "Persistent volume size for Alertmanager data"
#       type: "string"
#       default: "10Gi"

- name: Deploy Alertmanager
  hosts: "{{ target_hosts | default('localhost') }}"
  gather_facts: true
  become: false

  vars:
    app_name: alertmanager
    app_namespace: "alertmanager"
    templates_dir: "{{ playbook_dir }}/../templates"
    render_dir: "/tmp/alertmanager-manifests"
    kubectl_action: "{{ 'replace --force' if install_mode | default('rebuild') == 'full' else 'apply' }}"
    container_port: 9093
    health_path: "/-/healthy"

  tasks:
    - name: Verify kubectl is available
      ansible.builtin.command: kubectl version --client
      register: kubectl_check
      changed_when: false

    - name: Verify K3s cluster is accessible
      ansible.builtin.command: kubectl get nodes
      register: nodes_check
      changed_when: false
      retries: 5
      delay: 10
      until: nodes_check.rc == 0

    - name: Create render directory
      ansible.builtin.file:
        path: "{{ render_dir }}"
        state: directory
        mode: '0755'

    - name: Render ConfigMap template
      ansible.builtin.template:
        src: "{{ templates_dir }}/configmap.yaml.j2"
        dest: "{{ render_dir }}/configmap.yaml"
        mode: '0644'

    - name: Render StatefulSet template
      ansible.builtin.template:
        src: "{{ templates_dir }}/statefulset.yaml.j2"
        dest: "{{ render_dir }}/statefulset.yaml"
        mode: '0644'

    - name: Render Service template
      ansible.builtin.template:
        src: "{{ templates_dir }}/service.yaml.j2"
        dest: "{{ render_dir }}/service.yaml"
        mode: '0644'

    - name: Ensure namespace exists
      ansible.builtin.shell: |
        kubectl create namespace {{ app_namespace }} --dry-run=client -o yaml | kubectl apply -f -
      register: ns_apply
      changed_when: "'created' in ns_apply.stdout or 'configured' in ns_apply.stdout"

    - name: Label namespace
      ansible.builtin.command: >-
        kubectl label namespace {{ app_namespace }}
        app.kubernetes.io/name={{ app_name }}
        app.kubernetes.io/part-of=golden-master
        --overwrite
      changed_when: false

    # Split-apply: protected kinds always use kubectl apply (never replace --force).
    # Prevents namespace cascade deletion and secret rotation on rebuild.
    - name: Apply protected resources (Namespace, Secret, PVC, PushSecret)
      ansible.builtin.shell: |
        for f in {{ render_dir }}/*.yaml; do
          kind=$(grep -m1 '^kind:' "$f" 2>/dev/null | awk '{print $2}')
          case "$kind" in
            Namespace|Secret|PersistentVolumeClaim|PushSecret|ClusterRole|ClusterRoleBinding)
              kubectl apply -f "$f" 2>&1 && echo "[APPLIED] $f"
              ;;
          esac
        done
      register: protected_apply
      changed_when: "'created' in protected_apply.stdout or 'configured' in protected_apply.stdout"

    - name: Apply workload resources
      ansible.builtin.shell: |
        for f in {{ render_dir }}/*.yaml; do
          kind=$(grep -m1 '^kind:' "$f" 2>/dev/null | awk '{print $2}')
          case "$kind" in
            Namespace|Secret|PersistentVolumeClaim|PushSecret|ClusterRole|ClusterRoleBinding)
              ;;  # already applied above
            *)
              kubectl {{ kubectl_action }} -f "$f" 2>&1 && echo "[APPLIED] $f"
              ;;
          esac
        done
      register: workload_apply
      changed_when: "'created' in workload_apply.stdout or 'configured' in workload_apply.stdout"

    - name: Wait for pod to be ready
      ansible.builtin.command: >-
        kubectl -n {{ app_namespace }} wait
        --for=condition=ready pod
        -l app.kubernetes.io/name={{ app_name }}
        --timeout=600s
      register: pod_wait
      changed_when: false
      retries: 3
      delay: 10
      until: pod_wait.rc == 0

    - name: Get Tailscale IP for access URL
      ansible.builtin.command: tailscale ip -4
      register: tailscale_ip
      changed_when: false
      failed_when: false

    - name: Display deployment status
      ansible.builtin.debug:
        msg:
          - "=========================================="
          - "Alertmanager Deployed Successfully"
          - "=========================================="
          - "Namespace: {{ app_namespace }}"
          - "Image: {{ image | default('prom/alertmanager:v0.27.0') }}"
          - "Access URL: http://{{ tailscale_ip.stdout | default(ansible_hostname) }}:{{ nodeport | default(30903) }}"
          - "=========================================="

    - name: Clean up render directory
      ansible.builtin.file:
        path: "{{ render_dir }}"
        state: absent
