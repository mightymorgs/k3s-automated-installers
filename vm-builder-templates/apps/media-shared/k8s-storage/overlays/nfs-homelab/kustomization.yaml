---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: media-stack

resources:
  - namespace.yaml
  - nfs-pv.yaml
  - nfs-pvc.yaml

commonLabels:
  app.kubernetes.io/managed-by: kustomize
  storage-backend: nfs-homelab
