schema_version: '1.0'
repo_id: vm-builder-templates
files:
  01-deploy.yml:
    summary: 'Deploy shared media storage PVC for media stack applications. Supports
      three backends: - longhorn: Creates Longhorn RWX volume (for cloud deployments)
      - nfs: Mounts custom NFS share (requires server IP and path) - smb: Mounts SMB/CIFS
      share (requires server, share, username, password) Required by: sonarr, radarr,
      sabnzbd, qbittorrent.'
    source: inferred
    confidence: medium
    uses_files:
    - '{{ templates_dir }}/smb-pv.yaml.j2'
    - '{{ templates_dir }}/smb-pvc.yaml.j2'
