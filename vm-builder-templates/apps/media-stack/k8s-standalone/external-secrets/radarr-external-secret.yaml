---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: radarr-secrets
  namespace: media-stack
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: radarr-secrets
    creationPolicy: Owner
  data:
    - secretKey: api_key
      remoteRef:
        key: secret/apps/media-stack/radarr
        property: api_key
    - secretKey: url
      remoteRef:
        key: secret/apps/media-stack/radarr
        property: url
    - secretKey: nodeport_url
      remoteRef:
        key: secret/apps/media-stack/radarr
        property: nodeport_url
