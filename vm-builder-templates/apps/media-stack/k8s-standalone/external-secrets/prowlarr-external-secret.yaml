---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: prowlarr-secrets
  namespace: media-stack
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: prowlarr-secrets
    creationPolicy: Owner
  data:
    - secretKey: api_key
      remoteRef:
        key: secret/apps/media-stack/prowlarr
        property: api_key
    - secretKey: url
      remoteRef:
        key: secret/apps/media-stack/prowlarr
        property: url
    - secretKey: nodeport_url
      remoteRef:
        key: secret/apps/media-stack/prowlarr
        property: nodeport_url
