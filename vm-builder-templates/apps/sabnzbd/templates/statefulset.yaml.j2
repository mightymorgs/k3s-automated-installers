---
# SABnzbd StatefulSet (J2 template)
# Defaults match k8s-standalone/base/statefulset.yaml
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ app_name | default('sabnzbd') }}
  namespace: {{ app_namespace | default('media-stack') }}
  labels:
    app: {{ app_name | default('sabnzbd') }}
    app.kubernetes.io/name: {{ app_name | default('sabnzbd') }}
    app.kubernetes.io/component: downloader
    app.kubernetes.io/part-of: media-stack
spec:
  serviceName: {{ app_name | default('sabnzbd') }}
  replicas: {{ replicas | default(1) }}
  selector:
    matchLabels:
      app: {{ app_name | default('sabnzbd') }}
  template:
    metadata:
      labels:
        app: {{ app_name | default('sabnzbd') }}
        app.kubernetes.io/name: {{ app_name | default('sabnzbd') }}
    spec:
      containers:
        - name: {{ app_name | default('sabnzbd') }}
          image: {{ image | default('lscr.io/linuxserver/sabnzbd:latest') }}
          imagePullPolicy: {{ image_pull_policy | default('IfNotPresent') }}
          env:
            - name: PUID
              value: "{{ puid | default('1000') }}"
            - name: PGID
              value: "{{ pgid | default('1000') }}"
            - name: TZ
              value: "{{ timezone | default('Australia/Sydney') }}"
          ports:
            - name: http
              containerPort: {{ container_port | default(8080) }}
              protocol: TCP
          volumeMounts:
            - name: config
              mountPath: /config
            - name: shared-media
              mountPath: {{ downloads_mount_path | default('/downloads') }}
              subPath: {{ downloads_subpath | default('downloads') }}
          resources:
            requests:
              cpu: {{ cpu_request | default('200m') }}
              memory: {{ memory_request | default('512Mi') }}
            limits:
              cpu: {{ cpu_limit | default('2000m') }}
              memory: {{ memory_limit | default('2Gi') }}
          livenessProbe:
            httpGet:
              path: {{ health_path | default('/api?mode=version') }}
              port: http
            initialDelaySeconds: {{ liveness_initial_delay | default(60) }}
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: {{ health_path | default('/api?mode=version') }}
              port: http
            initialDelaySeconds: {{ readiness_initial_delay | default(30) }}
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
      volumes:
        - name: shared-media
          persistentVolumeClaim:
            claimName: {{ media_pvc_name | default('media-shared-pvc') }}
  volumeClaimTemplates:
    - metadata:
        name: config
        labels:
          app: {{ app_name | default('sabnzbd') }}
      spec:
        accessModes: ["ReadWriteOnce"]
        storageClassName: {{ storage_class | default('longhorn') }}
        resources:
          requests:
            storage: {{ config_storage_size | default('1Gi') }}
