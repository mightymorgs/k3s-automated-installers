---
apiVersion: v1
kind: Service
metadata:
  name: vault-mcp
  namespace: mcp-servers
  labels:
    app.kubernetes.io/name: vault-mcp
    app.kubernetes.io/component: ai-operations
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: vault-mcp
    app.kubernetes.io/component: ai-operations
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
