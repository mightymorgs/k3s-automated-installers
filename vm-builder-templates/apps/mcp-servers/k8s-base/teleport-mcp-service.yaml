---
apiVersion: v1
kind: Service
metadata:
  name: teleport-mcp
  namespace: mcp-servers
  labels:
    app.kubernetes.io/name: teleport-mcp
    app.kubernetes.io/component: ai-operations
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: teleport-mcp
    app.kubernetes.io/component: ai-operations
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
