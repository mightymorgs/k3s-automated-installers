---
- name: "Create Kind cluster"
  ansible.builtin.import_playbook: ../shared/create-kind.yml

- name: "Prepare namespaces"
  ansible.builtin.import_playbook: ../shared/prepare-namespaces.yml

- name: "Ensure jellyfin namespace"
  hosts: localhost
  gather_facts: false
  environment:
    KUBECONFIG: /tmp/molecule-kubeconfig
  tasks:
    - name: Create jellyfin namespace
      ansible.builtin.shell: >-
        kubectl create namespace jellyfin --dry-run=client -o yaml | kubectl apply -f -
      changed_when: false

- name: "Deploy jellyfin"
  ansible.builtin.import_playbook: ../../../apps/jellyfin/install/01-deploy.yml
