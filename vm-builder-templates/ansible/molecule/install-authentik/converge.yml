---
- name: "Create Kind cluster"
  ansible.builtin.import_playbook: ../shared/create-kind.yml

- name: "Prepare namespaces"
  ansible.builtin.import_playbook: ../shared/prepare-namespaces.yml

- name: "Ensure authentik namespace"
  hosts: localhost
  gather_facts: false
  environment:
    KUBECONFIG: /tmp/molecule-kubeconfig
  tasks:
    - name: Create authentik namespace
      ansible.builtin.shell: >-
        kubectl create namespace authentik --dry-run=client -o yaml | kubectl apply -f -
      changed_when: false

- name: "Deploy authentik"
  ansible.builtin.import_playbook: ../../../apps/authentik/install/01-deploy.yml
