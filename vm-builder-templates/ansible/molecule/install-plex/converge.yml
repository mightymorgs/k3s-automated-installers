---
- name: "Create Kind cluster"
  ansible.builtin.import_playbook: ../shared/create-kind.yml

- name: "Prepare namespaces"
  ansible.builtin.import_playbook: ../shared/prepare-namespaces.yml

- name: "Ensure plex namespace"
  hosts: localhost
  gather_facts: false
  environment:
    KUBECONFIG: /tmp/molecule-kubeconfig
  tasks:
    - name: Create plex namespace
      ansible.builtin.shell: >-
        kubectl create namespace plex --dry-run=client -o yaml | kubectl apply -f -
      changed_when: false

- name: "Deploy plex"
  ansible.builtin.import_playbook: ../../../apps/plex/install/01-deploy.yml
