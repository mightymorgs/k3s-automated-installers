---
- name: "Create Kind cluster"
  ansible.builtin.import_playbook: ../shared/create-kind.yml

- name: "Prepare namespaces"
  ansible.builtin.import_playbook: ../shared/prepare-namespaces.yml

- name: "Ensure elastic namespace"
  hosts: localhost
  gather_facts: false
  environment:
    KUBECONFIG: /tmp/molecule-kubeconfig
  tasks:
    - name: Create elastic namespace
      ansible.builtin.shell: >-
        kubectl create namespace logging --dry-run=client -o yaml | kubectl apply -f -
      changed_when: false

- name: "Deploy elastic"
  ansible.builtin.import_playbook: ../../../apps/elastic/install/01-apply-kustomize.yml
