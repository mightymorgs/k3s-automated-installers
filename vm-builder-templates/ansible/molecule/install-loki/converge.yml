---
- name: "Create Kind cluster"
  ansible.builtin.import_playbook: ../shared/create-kind.yml

- name: "Prepare namespaces"
  ansible.builtin.import_playbook: ../shared/prepare-namespaces.yml

- name: "Ensure loki namespace"
  hosts: localhost
  gather_facts: false
  environment:
    KUBECONFIG: /tmp/molecule-kubeconfig
  tasks:
    - name: Create loki namespace
      ansible.builtin.shell: >-
        kubectl create namespace observability --dry-run=client -o yaml | kubectl apply -f -
      changed_when: false

- name: "Deploy loki"
  ansible.builtin.import_playbook: ../../../apps/loki/install/01-deploy.yml
