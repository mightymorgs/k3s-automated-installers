"""IDI (Intent-Driven Infrastructure) — deterministic automation from natural language.

Turns natural language intents into deterministic infrastructure automation
by chaining atomic skills (400-800 tokens each) backed by a Neo4j graph.
"""

__version__ = "0.2.0"
